# AI 协作规则

## 适用范围

本文件适用于整个仓库，定义 AI 的执行方式。用户的明确要求优先于本文件的通用规则；本文件管执行流程与安全，
`docs/ARCHITECTURE.md` 管当前生效的架构与公共契约，`docs/STYLE.md` 管代码写法。子目录可用自己的 `AGENTS.md` 追加规则。
同一职责内的规则发生冲突时，停下来向用户确认，不自行取舍；规则未覆盖且涉及不可逆操作、外部影响或公共行为时，同样先确认。

## 文档分工

每类规则只有一个权威来源。修改规则时只改对应文件；其他文档可以摘要事实或引用权威来源，但不能引入额外规则。
出现冲突时以表中对应的权威来源为准。

| 文件 | 唯一职责 |
| --- | --- |
| `AGENTS.md` | AI 执行方式：修改流程、必须确认的变更、测试与验证档位 |
| `docs/ARCHITECTURE.md` | 当前生效的架构与公共契约：技术路线、包结构、依赖纪律、接口约定 |
| `docs/STYLE.md` | 代码格式与写法约定 |
| `README.md` | 项目说明、目录结构、构建与运行命令、提交规范 |
| `docs/CHANGELOG.md` | 版本变化 |
| `CLAUDE.md` | Claude Code 的规则入口，只引用本文件，不维护规则 |

## 项目事实

- 技术路线为 Java 25、Spring Boot 4.1.x 与 Maven；实际版本以 `pom.xml` 为准，开发环境 Maven 版本以 `README.md` 为准。
- 基础包名为 `com.wang.platform`，架构为模块化单体。包结构、依赖方向、跨模块协作契约、
  DTO/Query/VO/Entity 分工、Lombok 用法、统一结构化响应字段，全部以 `docs/ARCHITECTURE.md` 为准；
  该文档未覆盖的细节不自行补全规范。
- 项目只使用已引入的框架与组件，基础设施的引入状态以 `docs/ARCHITECTURE.md` 为准；引入或变更按「必须先确认的变更」执行。
- 默认使用中文交流和编写文档；代码标识符、包名、配置键与 API 字段使用英文。

## 修改前

1. 首次进入仓库，或修改依赖、配置、公共行为、跨模块代码时，阅读 `README.md`、`pom.xml`、
   `docs/ARCHITECTURE.md`、`docs/CHANGELOG.md`；局部修改至少阅读直接相关的生产代码、测试与文档；
   编写或调整 Java 代码前阅读 `docs/STYLE.md`。
2. 检查当前 Git 差异，保留与任务无关的用户修改。常规差异审查以 Git 可见的跟踪文件和未忽略文件为准；
   `.gitignore` 已忽略的本地文件或目录不作为常规审查、统计和汇报对象。只有用户明确要求、任务本身涉及
   `.gitignore` / 本地配置 / 生成产物，或需要排查密钥误提交、构建产物污染、被忽略文件影响运行结果等风险时，
   才按最小范围检查相关忽略文件。
3. 依据用户请求与现有约束整理可验证的验收条件。项目不单独维护需求文档；只有缺失信息会实质影响业务规则、公共 API、数据迁移或安全策略时才向用户确认，不为可从上下文确定的局部实现反复提问，也不自行补充业务规则。
4. 涉及版本敏感的 API、配置、依赖兼容性，或对当前推荐用法不确定时，查阅与项目版本匹配的官方文档（Java、Spring Boot、Spring Framework、Jakarta 等），不依赖博客、第三方教程或旧版示例。

## 必须先确认的变更

用户请求已经明确包含相应变更时，无需重复确认。

- 新增或替换依赖、插件、框架、基础设施组件与外部服务：需说明用途、替代方案和影响。
- 启用虚拟线程、异步任务、定时任务、缓存、事务、AOT、原生镜像等改变运行模型、资源使用或部署方式的能力。
- 修改公共 API 的路径、请求方法、字段、状态码、错误结构、校验规则或序列化行为。
- 修改数据库结构、数据迁移、认证授权、跨域策略、时区语义、密钥管理、日志范围或隐私处理。
- 增加会阻止构建、提交或发布的质量门禁、代码生成与自动化流程。
- 使用预览、孵化、实验性或已废弃功能：同时记录采用原因、配置与退出方案。
- 大范围包重组，或新增业务模块。

采用 Java 25 与 Spring Boot 4.1 只表示允许使用与当前版本匹配的正式能力，不等于授权启用全部新特性、
新模块与默认配置。不改变公共行为、运行模型和技术路线的正式语法（模式匹配、`switch` 表达式、文本块等）
可按需使用，无需逐处确认。

## 编码依据

- 技术路线、依赖纪律、包结构、分层边界和公共契约以 `docs/ARCHITECTURE.md` 为准。
- 格式、空行、注释、Spring 编码方式、输入校验和日志写法以 `docs/STYLE.md` 为准。
- 实现与权威文档冲突时，不保留两套规则；按本文件的确认流程修改对应权威文档后再实现。

## 测试与验证

- 行为变更应在成本最低的合适层级增加测试，覆盖主要成功路径、边界与失败情况；修复缺陷时增加能复现问题的回归测试。
- 每个测试必须有针对行为的断言，禁止空测试和恒真断言；Spring Boot 上下文冒烟测试除外，启动成功本身即断言。
- 领域逻辑优先用普通单元测试，适配器优先用 Spring 测试切片，只有确实需要完整容器时才用 `@SpringBootTest`。
- 测试必须可重复，不依赖真实时间、随机结果、机器时区、固定端口或外部服务，也不使用随意的 sleep。
- 不得为了方便测试，在生产代码（`src/main`）中新增仅供测试调用的方法、访问器或分支；测试辅助方法与测试夹具
  必须放在测试代码（`src/test`）中，测试通过生产代码已有的对外行为进行验证。
- 未经用户明确同意，不得执行 `mvn spring-boot:run`、`java -jar`，也不得启动应用、服务器或其他长期运行进程。
- 测试类与测试方法的命名、断言库选择、用例粒度见 `docs/STYLE.md`「测试写法」。

验证档位：先判断改动是否改变程序行为，再选择成本最低且足够的一档，不默认执行 `clean`。

| 档位 | 适用改动 | 执行 |
| --- | --- | --- |
| 0 | 注释、Javadoc、纯格式调整、文档与文本文件 | 人工核对格式与引用；仅有 Java 非行为改动时最多执行 `mvn test-compile`，禁止运行测试 |
| 1 | 单个类或普通业务代码的行为变更 | 运行相关测试类，必要时 `mvn test` |
| 2 | 影响构建或启动行为的 `pom.xml` / 配置改动、跨模块公共代码的行为变更 | `mvn verify` |
| 3 | 发布前、重大依赖升级、怀疑旧构建产物干扰 | `mvn clean verify` |

无法确定改动属于哪一档时，选择更高的一档（高不就低）。该原则只适用于 1 档及以上；注释与格式改动不存在行为不确定性，不得据此升到 1 档。

同一任务同时包含多个档位的改动时，按其中最高档位统一验证；0 档限制只适用于全部改动都不改变行为的任务。

## Git 提交

- 提交格式、语言、粒度和构建产物规则遵守 `README.md`「Git 提交规范」。
- 未经用户明确要求，不创建或切换分支，不执行暂存、提交、打标签或推送操作。

## 完成条件

- 代码、测试、配置与文档保持一致；`README.md` 结构树注释与文档术语的一致性细则见 `docs/STYLE.md`「文档一致性」。
- 源码目录按 `docs/ARCHITECTURE.md` 按需创建；完成前检查 `src/main/java` 和 `src/test/java` 不存在空包目录，不使用 `.gitkeep` 保留空包。
- 影响长期维护、公共行为、运行模型或多个模块的技术选择，确认后写入 `docs/ARCHITECTURE.md`；普通实现细节不记录。
- 不提交密钥与生成产物。
- 汇报使用极简格式：修改了哪些文件、执行了哪些验证、结果如何；未执行某项验证时说明原因。
