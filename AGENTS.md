# AI 协作规则

## 适用范围

本文件适用于整个仓库。用户的明确要求，以及已经确认的需求和架构决策，优先于这里的通用规则。子目录如有自己的 `AGENTS.md`，可以为该目录补充规则。本文件内部规则之间发生冲突时，停下来向用户确认，不自行取舍。

## 项目事实

- 项目使用 Java 25、Spring Boot 4.1.x 和 Maven 3.9.16。
- 基础包名为 `com.wang.platform`。
- 引入数据库、缓存、消息队列、认证、微服务或部署方案等新基础设施组件前，必须先经用户确认，并将最终选择记录到 `docs/decisions.md`。
- 默认使用中文交流和编写文档；代码标识符、包名、配置键和 API 字段使用英文。
- Git 提交可使用英文类型加中文说明，例如 `feat: 增加用户登录`。
- 单次提交聚焦一个改动；不提交 `target/` 等构建产物。

## 修改前

1. 阅读 `README.md`、`pom.xml`、`docs/decisions.md`，以及附近的生产代码和测试。
2. 检查当前 Git 差异，保留与任务无关的用户修改。
3. 先明确可验证的验收条件。不得自行决定未说明的业务规则、公共 API、数据迁移或安全策略。
4. 参考与当前版本匹配的 Java、Spring Boot、Spring Framework 和 Jakarta 官方文档，不照搬旧版 Spring Boot 示例。

## Java 25 与 Spring Boot 4.1

- 不得为了让旧代码或示例通过编译而降低 Java 或 Spring Boot 版本。
- 在适合场景时使用已经正式发布的现代 Java 特性，例如 record、密封类、模式匹配、switch 表达式、文本块和虚拟线程。
- 不为“使用新特性”而增加复杂度。预览特性必须先经用户确认并记录到 `docs/decisions.md`，同时配置编译、测试、运行和 IDE 参数。
- 依赖和插件版本优先由 Spring Boot Parent 管理，不重复声明版本或配置。
- Jakarta EE API 使用 `jakarta.*`，不得生成旧的 `javax.*` 写法。
- 使用构造器注入，不使用字段注入，也不通过静态变量获取 Spring 容器。
- 安全配置参考当前版本官方文档，不使用已被删除的旧版 API（如 `WebSecurityConfigurerAdapter`）。
- 成组配置使用类型安全的 `@ConfigurationProperties`；密钥通过环境变量或密钥服务注入，不写入仓库。

## 设计与编码

- 代码按业务能力组织，同一业务的接口、应用逻辑、领域逻辑和基础设施代码保持接近。
- 同一模块在 README、配置、类与方法注释、测试及接口文档中必须使用统一的业务术语和职责描述；模块职责发生变化时同步更新所有相关说明。
- 注释使用简洁、准确的正式表述，重点说明职责、业务约束、输入输出或设计原因；不重复翻译代码，不记录作者、创建时间和修改历史。
- 控制器和外部适配器保持轻量；业务编排放在应用服务，业务约束放在领域代码。
- 不直接把持久化实体作为公共 API 的请求或响应模型。
- 在系统边界校验输入，在领域内部保证业务不变量；不得吞掉异常或用未说明的 `null` 表示失败。
- 优先使用小而明确的类型和组合，不提前创建通用基类、通用 Service/Controller、杂乱的 `utils` 或微服务。
- 金额使用 `BigDecimal` 并明确币种和舍入规则；时间使用 `java.time` 并明确时区语义。
- 使用参数化日志，不记录密码、令牌、会话标识、个人信息或完整请求体。
- JDK 或 Spring 已有清晰方案时不添加第三方依赖；新增依赖必须有具体用途并与当前版本兼容。
- 遵守 `.editorconfig` 和 `.gitattributes`，避免无意义的格式与换行变化。

## 测试与验证

- 未经用户明确同意，不得执行 `mvn spring-boot:run`、`java -jar`，也不得启动应用、服务器或其他长期运行进程。
- 行为变更应在成本最低的合适层级增加测试，并覆盖主要成功路径、边界和失败情况。
- 每个测试必须有针对行为的断言，禁止空测试和恒真断言；Spring Boot 上下文启动冒烟测试（`contextLoads`）除外，启动成功本身就是断言。
- 领域逻辑优先使用普通单元测试，适配器优先使用 Spring 测试切片；只有确实需要完整容器时才使用 `@SpringBootTest`。
- 测试必须可重复，不依赖真实时间、随机结果、机器时区、固定端口或外部服务，也不使用随意的 sleep。
- 修复缺陷时增加能够复现问题的回归测试。
- 按改动范围选择成本最低且足够的验证，不默认执行 `clean`；拿不准属于哪一档时就高不就低：
  - 只修改文档或注释：检查格式和引用即可，无需运行 Maven。
  - 修改单个类：优先编译并运行相关单元测试或测试类。
  - 修改普通业务代码：运行相关测试，必要时执行 `mvn test`。
  - 修改 `pom.xml`、启动配置或跨模块公共代码：执行 `mvn verify`。
  - 发布前、重大依赖升级或怀疑旧构建产物干扰时：执行 `mvn clean verify`。
- 完成时说明实际执行了哪些验证以及结果；没有执行相关测试时说明原因。

## 文档与完成条件

- 不强制建立单独的需求文档；需求不明确时直接向用户确认，不自行补充业务规则。
- 只有长期、跨模块的技术选择才追加到 `docs/decisions.md`，普通实现细节不记录。
- 已记录的技术决策不删除；方向变化时追加新条目并说明替代关系。
- 完成时确保代码、测试、配置和文档一致，不包含密钥与生成产物。
- 完成汇报使用极简格式：修改了哪些文件、执行了哪些验证、结果如何；未执行某项验证时说明原因。
